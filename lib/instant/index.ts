// InstantDB — all exports from a single place
export { default as db } from './client';
export { default as schema } from './schema';
export { default as rules } from './perms';
export type { AppSchema } from './schema';
